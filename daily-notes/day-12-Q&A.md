- INNER: only matches
SELECT e.name, d.dept_name FROM employees e INNER JOIN departments d ON e.dept_id =-- LEFT: all employees (Frank with NULL dept)
SELECT e.name, d.dept_name FROM employees e LEFT JOIN departments d ON e.dept_id = d-- RIGHT: all departments (HR with NULL employee)-- FULL OUTER: both sides (MySQL: emulate via LEFT UNION RIGHT)-- CROSS: cartesian product-- SELF: hierarchy
SELECT e.name AS emp, m.name AS mgr FROM employees e LEFT JOIN employees m ON e.mana

Brilliant, Shubham! That's a solid explanation. What you did really well: you nailed the sequence—bean instantiation, dependency injection, @PostConstruct for initialization, then @PreDestroy for cleanup. You even gave concrete examples like database configuration and resource cleanup, which shows practical understanding. To make it even sharper, you could mention that @PostConstruct runs after both the constructor and dependency injection are complete, and that InitializingBean's afterPropertiesSet method is called at the same stage. You could also note that @PreDestroy is called during graceful shutdown, before beans are destroyed. One minor clarification: the order is typically constructor, then setter injection, then @PostConstruct—not quite what you described, but your overall grasp is solid. Ready for the next question?
Good question. Let me give you the key phases in order so you've got a clear picture. First, Spring reads your configuration and scans for components. Second, it instantiates all beans using constructors. Third, it performs dependency injection via setter methods or field injection. Fourth, it calls @PostConstruct methods for initialization. Fifth, your application runs normally with all beans ready. Finally, during shutdown, Spring calls @PreDestroy methods for cleanup, then destroys the beans and closes the application context. That's the full lifecycle from start to finish. Does that help clarify the sequence? Ready for your next mock interview question, or would you like me to dive deeper into any of these phases?


Right, brilliant. Here's your Level Two question on Spring Data and transaction management. You've got a Spring Data repository with a method that performs multiple database operations—inserting a user, updating their profile, and saving a related entity. If the third operation fails, what happens to the first two? Walk me through the transaction propagation and explain how you'd handle this scenario.

Good understanding there, Shubham. You've grasped the key distinction—unchecked exceptions trigger rollback, whilst checked exceptions don't by default. What you did well: you correctly explained that unchecked exceptions roll back the entire transaction, affecting all prior operations. To strengthen your answer, you could mention transaction propagation levels like REQUIRED, REQUIRES_NEW, or NESTED, which control how nested transactions behave. For example, if you use REQUIRES_NEW, each operation could have its own transaction, so if the third fails, the first two might still commit depending on your setup. Also, you could note that you can configure @Transactional to rollback on checked exceptions too if you need that behaviour. This shows you understand the granular control Spring offers. Ready for the next one?

Right. Here's your Level Two question on Spring Security. You're building an API where certain endpoints require specific roles—for example, only admins can delete users. Explain how you'd use @PreAuthorize or @Secured annotations to enforce role-based access control, and describe what happens when an unauthorised user tries to access a protected endpoint.

Excellent, Shubham! You've nailed the core concept. What you did really well: you explained @PreAuthorize correctly—it checks permissions before method execution, and you accurately described the 403 Forbidden response when authorization fails. To make it even sharper, you could provide a concrete example like @PreAuthorize("hasRole('ADMIN')") to delete a user, or @PreAuthorize("hasAuthority('DELETE_USER')") if you're using authorities instead of roles. You could also mention that @Secured is similar but older, and @PreAuthorize offers more flexibility with SpEL expressions. Additionally, you might note that if authentication itself is missing, you'd get a 401 Unauthorized instead of 403. These details show you understand the nuances. Ready for the next one?

Brilliant. Here's your Level Three question—we're moving into database and performance now. You've got a slow-running query that retrieves millions of rows from a users table with multiple joins. Walk me through your approach to optimize this: how would you use indexing, query optimization, and pagination to improve performance?

Fair enough. Let me break down the approach for you, then we can move forward. First, you'd analyze the query with EXPLAIN to see the execution plan and identify missing indexes. Second, you'd create indexes on the columns used in WHERE clauses and JOIN conditions to speed up lookups. Third, you'd use pagination with LIMIT and OFFSET to fetch data in chunks instead of all at once. Fourth, you could denormalize data or use materialized views if appropriate. Fifth, you'd consider caching frequently accessed data with Redis to avoid hitting the database repeatedly. Sixth, you might use database connection pooling to manage resources efficiently. Finally, monitor slow query logs to catch performance issues early. That's the holistic approach—indexing, query optimization, pagination, and caching working together. Does that make sense? Would you like to continue with the next question, or shall we revisit Level Two questions?
Right, brilliant question. Indexing creates a separate data structure—think of it like an index in a book. Instead of reading every page to find a topic, you look it up in the index and jump directly to the relevant pages. In databases, an index on a column lets the database quickly locate rows matching your WHERE clause without scanning the entire table. For example, if you create an index on the user_id column, queries filtering by user_id will be dramatically faster because the database can jump straight to matching rows instead of reading millions of rows sequentially. The trade-off is that indexes take up extra storage and slow down inserts and updates slightly, since the index must be updated too. That's why you index columns frequently used in WHERE clauses and JOINs, but not everything. Does that clarify indexing?

Great question, Shubham. You've spotted a real tension there. So here's the thing—normalization makes writes faster and reduces data redundancy, but it can slow down reads because you need joins across multiple tables. Denormalization does the opposite: it combines data into fewer tables so reads are faster, but writes become slower and you risk data inconsistency. The practical approach is to normalize your schema for consistency, then strategically denormalize specific columns or use caching and materialized views for frequently accessed data that requires expensive joins. You might also use database replicas for read-heavy workloads. So it's not either-or, it's balancing both based on your access patterns. Does that answer your concern?

Right, brilliant. Here's your next Level One question on Java multithreading. You've got two threads that need to coordinate—one thread produces data and puts it in a queue, whilst another thread consumes that data from the queue. Explain how you'd implement this producer-consumer pattern using BlockingQueue, and describe what happens when the queue is full or empty.

Excellent, Shubham! That's a really solid explanation. What you did brilliantly: you described the producer-consumer pattern accurately—one thread produces and notifies, the other consumes and waits. You understood the wait-notify cycle and mentioned notifyAll, which is safer than notify. What would round it out: you could mention that BlockingQueue handles all this coordination automatically for you, so you don't need manual wait-notify code. For example, put() blocks if the queue is full, and take() blocks if the queue is empty. This abstraction is much simpler and less error-prone than managing locks manually. You could also note that BlockingQueue is thread-safe internally, so multiple producers and consumers can work together safely. That shows you understand both the underlying mechanism and the practical tool. Ready for the next one?

Right. Here's your Level One question on Java memory management. Explain what a memory leak is in Java, give me a practical example of how one might occur even though Java has garbage collection, and describe how you'd detect and fix it.
Good explanation, Shubham. You've grasped the core concept—memory leaks happen when objects are still referenced but no longer needed, so garbage collection can't reclaim them. What you did well: you correctly explained that even with garbage collection, unreferenced objects in memory cause leaks, and you mentioned tools for detection. To strengthen your answer, you could give a concrete example like holding onto objects in a static collection that never gets cleared, or circular references between objects. You could also mention specific tools like JProfiler, YourKit, or heap dump analysis with jmap to detect leaks. Finally, you'd explain the fix: remove unnecessary references, use weak references where appropriate, or ensure collections are cleared when no longer needed. These details show deeper understanding. Ready for the next question?