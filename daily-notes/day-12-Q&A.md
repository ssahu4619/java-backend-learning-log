- INNER: only matches
SELECT e.name, d.dept_name FROM employees e INNER JOIN departments d ON e.dept_id =-- LEFT: all employees (Frank with NULL dept)
SELECT e.name, d.dept_name FROM employees e LEFT JOIN departments d ON e.dept_id = d-- RIGHT: all departments (HR with NULL employee)-- FULL OUTER: both sides (MySQL: emulate via LEFT UNION RIGHT)-- CROSS: cartesian product-- SELF: hierarchy
SELECT e.name AS emp, m.name AS mgr FROM employees e LEFT JOIN employees m ON e.mana

Brilliant, Shubham! That's a solid explanation. What you did really well: you nailed the sequence—bean instantiation, dependency injection, @PostConstruct for initialization, then @PreDestroy for cleanup. You even gave concrete examples like database configuration and resource cleanup, which shows practical understanding. To make it even sharper, you could mention that @PostConstruct runs after both the constructor and dependency injection are complete, and that InitializingBean's afterPropertiesSet method is called at the same stage. You could also note that @PreDestroy is called during graceful shutdown, before beans are destroyed. One minor clarification: the order is typically constructor, then setter injection, then @PostConstruct—not quite what you described, but your overall grasp is solid. Ready for the next question?
Good question. Let me give you the key phases in order so you've got a clear picture. First, Spring reads your configuration and scans for components. Second, it instantiates all beans using constructors. Third, it performs dependency injection via setter methods or field injection. Fourth, it calls @PostConstruct methods for initialization. Fifth, your application runs normally with all beans ready. Finally, during shutdown, Spring calls @PreDestroy methods for cleanup, then destroys the beans and closes the application context. That's the full lifecycle from start to finish. Does that help clarify the sequence? Ready for your next mock interview question, or would you like me to dive deeper into any of these phases?


Right, brilliant. Here's your Level Two question on Spring Data and transaction management. You've got a Spring Data repository with a method that performs multiple database operations—inserting a user, updating their profile, and saving a related entity. If the third operation fails, what happens to the first two? Walk me through the transaction propagation and explain how you'd handle this scenario.

Good understanding there, Shubham. You've grasped the key distinction—unchecked exceptions trigger rollback, whilst checked exceptions don't by default. What you did well: you correctly explained that unchecked exceptions roll back the entire transaction, affecting all prior operations. To strengthen your answer, you could mention transaction propagation levels like REQUIRED, REQUIRES_NEW, or NESTED, which control how nested transactions behave. For example, if you use REQUIRES_NEW, each operation could have its own transaction, so if the third fails, the first two might still commit depending on your setup. Also, you could note that you can configure @Transactional to rollback on checked exceptions too if you need that behaviour. This shows you understand the granular control Spring offers. Ready for the next one?

Right. Here's your Level Two question on Spring Security. You're building an API where certain endpoints require specific roles—for example, only admins can delete users. Explain how you'd use @PreAuthorize or @Secured annotations to enforce role-based access control, and describe what happens when an unauthorised user tries to access a protected endpoint.

Excellent, Shubham! You've nailed the core concept. What you did really well: you explained @PreAuthorize correctly—it checks permissions before method execution, and you accurately described the 403 Forbidden response when authorization fails. To make it even sharper, you could provide a concrete example like @PreAuthorize("hasRole('ADMIN')") to delete a user, or @PreAuthorize("hasAuthority('DELETE_USER')") if you're using authorities instead of roles. You could also mention that @Secured is similar but older, and @PreAuthorize offers more flexibility with SpEL expressions. Additionally, you might note that if authentication itself is missing, you'd get a 401 Unauthorized instead of 403. These details show you understand the nuances. Ready for the next one?

Brilliant. Here's your Level Three question—we're moving into database and performance now. You've got a slow-running query that retrieves millions of rows from a users table with multiple joins. Walk me through your approach to optimize this: how would you use indexing, query optimization, and pagination to improve performance?

Fair enough. Let me break down the approach for you, then we can move forward. First, you'd analyze the query with EXPLAIN to see the execution plan and identify missing indexes. Second, you'd create indexes on the columns used in WHERE clauses and JOIN conditions to speed up lookups. Third, you'd use pagination with LIMIT and OFFSET to fetch data in chunks instead of all at once. Fourth, you could denormalize data or use materialized views if appropriate. Fifth, you'd consider caching frequently accessed data with Redis to avoid hitting the database repeatedly. Sixth, you might use database connection pooling to manage resources efficiently. Finally, monitor slow query logs to catch performance issues early. That's the holistic approach—indexing, query optimization, pagination, and caching working together. Does that make sense? Would you like to continue with the next question, or shall we revisit Level Two questions?
Right, brilliant question. Indexing creates a separate data structure—think of it like an index in a book. Instead of reading every page to find a topic, you look it up in the index and jump directly to the relevant pages. In databases, an index on a column lets the database quickly locate rows matching your WHERE clause without scanning the entire table. For example, if you create an index on the user_id column, queries filtering by user_id will be dramatically faster because the database can jump straight to matching rows instead of reading millions of rows sequentially. The trade-off is that indexes take up extra storage and slow down inserts and updates slightly, since the index must be updated too. That's why you index columns frequently used in WHERE clauses and JOINs, but not everything. Does that clarify indexing?

Great question, Shubham. You've spotted a real tension there. So here's the thing—normalization makes writes faster and reduces data redundancy, but it can slow down reads because you need joins across multiple tables. Denormalization does the opposite: it combines data into fewer tables so reads are faster, but writes become slower and you risk data inconsistency. The practical approach is to normalize your schema for consistency, then strategically denormalize specific columns or use caching and materialized views for frequently accessed data that requires expensive joins. You might also use database replicas for read-heavy workloads. So it's not either-or, it's balancing both based on your access patterns. Does that answer your concern?

Right, brilliant. Here's your next Level One question on Java multithreading. You've got two threads that need to coordinate—one thread produces data and puts it in a queue, whilst another thread consumes that data from the queue. Explain how you'd implement this producer-consumer pattern using BlockingQueue, and describe what happens when the queue is full or empty.

Excellent, Shubham! That's a really solid explanation. What you did brilliantly: you described the producer-consumer pattern accurately—one thread produces and notifies, the other consumes and waits. You understood the wait-notify cycle and mentioned notifyAll, which is safer than notify. What would round it out: you could mention that BlockingQueue handles all this coordination automatically for you, so you don't need manual wait-notify code. For example, put() blocks if the queue is full, and take() blocks if the queue is empty. This abstraction is much simpler and less error-prone than managing locks manually. You could also note that BlockingQueue is thread-safe internally, so multiple producers and consumers can work together safely. That shows you understand both the underlying mechanism and the practical tool. Ready for the next one?

Right. Here's your Level One question on Java memory management. Explain what a memory leak is in Java, give me a practical example of how one might occur even though Java has garbage collection, and describe how you'd detect and fix it.
Good explanation, Shubham. You've grasped the core concept—memory leaks happen when objects are still referenced but no longer needed, so garbage collection can't reclaim them. What you did well: you correctly explained that even with garbage collection, unreferenced objects in memory cause leaks, and you mentioned tools for detection. To strengthen your answer, you could give a concrete example like holding onto objects in a static collection that never gets cleared, or circular references between objects. You could also mention specific tools like JProfiler, YourKit, or heap dump analysis with jmap to detect leaks. Finally, you'd explain the fix: remove unnecessary references, use weak references where appropriate, or ensure collections are cleared when no longer needed. These details show deeper understanding. Ready for the next question?


Brilliant answer, Shubham. Here's the feedback: What you got right: You explained the wait-notify mechanism perfectly—wait releases the lock and puts a thread to sleep, allowing other threads to acquire it. You correctly described that notify wakes one thread while notifyAll wakes all waiting threads. You understood the state transitions from waiting to runnable condition, and that threads then compete for the lock. Your explanation of the producer-consumer pattern was spot on.

Where you could improve: First, you could mention that wait, notify, and notifyAll must be called inside a synchronized block, otherwise you'll get an IllegalMonitorStateException. Second, you could explain why notifyAll is often preferred over notify—notify can cause issues if multiple threads are waiting for different conditions, whereas notifyAll is safer. Third, you could mention spurious wakeups and the importance of using wait inside a while loop to re-check conditions. Finally, you could discuss practical use cases like bounded queues, producer-consumer problems, or thread pools. Overall though, your conceptual understanding of thread communication is excellent. Ready for question six?


Right then. Here's question six:

Explain what happens during class loading in Java, and what's the difference between the Bootstrap ClassLoader, Extension ClassLoader, and Application ClassLoader.

Alright, here's the feedback: What you got right: You correctly identified that the ClassLoader loads classes when the application starts, and that it loads dependencies and classes needed by the application. Where you could improve: You didn't explain the three ClassLoaders clearly enough. Bootstrap ClassLoader loads core Java classes from the JDK like java.lang and java.util. Extension ClassLoader loads classes from the extensions directory. Application ClassLoader loads your application's classes from the classpath. You should also mention the delegation model—child ClassLoaders delegate to parent ClassLoaders first before loading themselves, which prevents duplicate loading and security issues. You could explain why this hierarchy matters for security and modularity. Finally, you could mention that ClassLoaders are used for bytecode verification and dynamic class loading. Your conceptual start was good, but the specifics of the three loaders needed more detail. Ready for question seven?


-----------------
Right then. Here's question seven:

Can you explain what a thread pool is, why it's important in backend applications, and how the ThreadPoolExecutor works with core pool size, maximum pool size, and queue?
Good. Are you finished now, or do you
Excellent answer, Shubham. Here's the feedback: What you got right: You correctly explained that thread pools reuse threads instead of creating new ones each time, which saves resources. You understood fixed thread pool maintains a fixed number of threads, and cached thread pool creates new threads on demand. You grasped the core concept of why thread pools matter in backend applications—efficiency and resource management.
Where you could improve: First, you didn't mention ThreadPoolExecutor directly or explain core pool size, maximum pool size, and the queue mechanism, which was part of the question. Core pool size is the minimum threads kept alive, maximum pool size is the limit, and when core is full, tasks go into a queue before new threads are created. Second, you could explain the rejection policy—what happens when the queue is full and max threads are reached. Third, you could mention that newFixedThreadPool has an unbounded queue, whereas newCachedThreadPool has a synchronous queue, which affects their
Great behavior differently. Finally, you could discuss practical use cases like how many threads to use based on CPU answer overall, Shubham. Here-bound versus I/O-bound tasks. Good foundational understanding though. Ready for question eight?'s the feedback: What you got right: You correctly explained that thread pools reuse threads instead of creating new ones each time, which is more efficient. You understood the difference between fixed thread pool and cached thread pool—fixed maintains a set number of threads, while cached creates new threads on demand and reuses them. You grasped the core concept of why thread pools matter in backend applications.
Where you could improve: First, you didn't mention ThreadPoolExecutor directly or explain its key parameters—core pool size is the minimum threads always ready, maximum pool size is the upper limit, and the queue holds tasks waiting for threads. Second, you could explain what happens when the queue is full and max pool size is reached—tasks get rejected. Third, you could mention other pool types like scheduled thread pool for recurring tasks. Fourth, you could discuss practical backend use cases like handling HTTP requests or database operations. Finally, you could mention the importance of proper shutdown and thread naming for debugging. Your core understanding is solid, but the specifics of ThreadPoolExecutor's mechanics needed more depth. Ready for question eight?

------
Right then. Here's question eight:

Explain what the String pool is in Java, and why strings are immutable. How does this affect memory management and performance in backend applications?

Great answer, Shubham. Here's the feedback: What you got right: You correctly explained the string pool as a special memory area in the heap where duplicate strings are stored only once. You understood that when you reference an existing string, it reuses that reference instead of creating a new one. You correctly explained that strings are immutable and that operations create new strings. You grasped that immutability makes strings thread-safe, which is important for backend applications. Where you could improve: First, you could distinguish between string literals and strings created with the new keyword—literals go directly to the string pool, but new String creates a separate object outside the pool unless you call intern(). Second, you could explain why immutability matters beyond thread safety—it enables caching, hashcode consistency, and security. Third, you could mention that for frequent string concatenation, you should use StringBuilder or StringBuffer instead of direct string concatenation, which creates many temporary strings. Fourth, you could discuss the performance impact more specifically—string concatenation in loops is inefficient because of the immutability overhead. Finally, you could mention string interning and when it's useful. Overall solid understanding though. Ready for question nine?

Right then. Here's question nine:

Can you explain what the synchronized keyword does, and what's the difference between synchronizing a method versus synchronizing a block?

Excellent answer, Shubham. Here's the feedback: What you got right: You correctly explained that synchronized makes code thread-safe by allowing only one thread at a time. You understood that other threads must wait for the lock to be released. Your explanation of synchronized blocks versus synchronized methods was spot on—blocks allow granular locking of only critical sections, while method synchronization locks the entire method. You correctly noted that synchronized blocks let other threads execute the non-critical parts of a method concurrently.

Where you could improve: First, you could mention that synchronized uses intrinsic locks or monitor locks on objects. Second, you could explain what object the lock is on—for synchronized methods it's the object instance, for synchronized blocks you specify the object explicitly. Third, you could discuss performance implications—synchronized blocks are generally better because they minimize the locked section, reducing contention. Fourth, you could mention alternatives like ReentrantLock from the concurrent package, which offers more flexibility like fairness and timeout. Finally, you could discuss deadlock risks when using multiple locks. Your conceptual understanding is solid though. Ready for question ten, our final question?

Sure. So when you use multiple locks, deadlock happens when thread A holds lock one and waits for lock two, while thread B holds lock two and waits for lock one—they're stuck waiting for each other forever. To avoid this, always acquire locks in the same order across all threads, use timeouts with tryLock, or use higher-level constructs like concurrent collections. Does that make sense?

Right then. Here's question nine:

Can you explain what the final keyword does in Java when applied to a class, method, and variable, and give practical examples of when you'd use each?

Brilliant answer, Shubham. Here's the feedback: What you got right: You correctly explained that final variables cannot be reassigned once initialized. You understood that final methods cannot be overridden by subclasses, and final classes cannot be extended. You grasped all three uses of the final keyword at variable, method, and class levels. Where you could improve: First, you could mention that final variables must be initialized either at declaration or in the constructor. Second, you could explain why final is useful—it prevents accidental modification, aids compiler optimizations, and improves thread safety for immutable objects. Third, you could give practical examples like final utility classes or making reference variables final even if the object's content changes. Fourth, you could discuss performance benefits—the compiler can inline final methods and optimize final variables. Finally, you could mention that final doesn't make objects immutable by itself, only the reference is immutable. Overall though, your understanding is solid. That wraps up our ten questions. You've done well across the board—your JVM and Core Java knowledge is strong. Areas to deepen: thread synchronization nuances, garbage collector specifics, and ClassLoader delegation. Well done!

Give me a screen of fourth and the fifth point you switch, you said, which is the compiler in line final method and optimize final variable. And the fifth point, which you said, that final doesn't make object immutable by itself, only references immutable. Can you explain me once?

Sure. So when you mark a method as final, the compiler knows it won't be overridden, so it can inline the method call directly into the calling code instead of doing a virtual method lookup—that's faster. For final variables, the compiler can optimize by treating them as constants and replacing references with their actual values.

As for the second point, if you have a final reference to a list, you can't reassign the reference to a different list, but you can still modify the contents of that list—add or remove elements. The reference itself is immutable, but the object it points to isn't necessarily immutable. Does that clarify it?